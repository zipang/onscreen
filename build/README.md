To build the packed, minified version of the software, you must first have node.js installed and the buildify package :

    npm install buildify

Then, run the build executable.

    node build.js
    